#include <stdio.h>

/**
 * main - prints the program name
 * @argc: input integer that is not used
 * @argv: input integer
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
