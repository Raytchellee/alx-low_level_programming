#include "main.h"

/**
 *_abs - returns absolute of a number
 *@n: input to check
 *Return: return abs of number on success
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
