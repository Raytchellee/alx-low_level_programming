#include <stdio.h>
/**
 * main - prints all the small letters of the alphabet except q and e.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alph = "a";
	
	while (alph <= 'z')
	{
		if (char != 'e' && char != 'q')
		{
			putchar(aplh);
			alph++;
		}
	}
	putchar('\n');
	return (0);
}
